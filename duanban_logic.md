# 断板反包模块 · 判断逻辑说明（确认池 + 观察池）

> 适用版本：R89（在 R87 双池 + R88 打标制 + 四均线 K线 基础上，新增"依据小作文化" story 字段、K 线柱子点击看涨幅）
> 核心脚本：`check_duanban.py`（确定性形态核验，零 MCP，纯 HTTP）
> 协作脚本：`merge_duanban.py`（AI 草稿合并进 `data.js`）、`validate_stocks.py`（结构校验闸门）

---

## 1. 一句话定位

"断板反包"= 一只主板票 **涨停（T 日）→ 次日放量断板（没再封板）→ 随后缩量企稳不破位 → 随时可能反包续涨** 的形态。
本模块用确定性脚本把全市场近期出现过这种形态的票筛出来，分成 **确认池（形态已走完）** 和 **观察池（差最后一根确认 K 线）**，再与当日要闻 / AI 预测交叉打标（利好 / 利空 / 中性），最后由自动化 AI 补上涨幅概率与个股层面小作文。

---

## 2. 数据流（端到端）

```
每日 16:00 A股收盘自动化（07:30 美股 / 周日23:00 周末亦更新）
   │
   ├─① check_duanban.py --module --out draft.json
   │      a. recent_trade_dates(5)  取最近 5 个工作日
   │      b. fetch_zt_pool(d)       逐日拉东财涨停池（含行业板块 hybk）
   │      c. get_kline(code)        腾讯日K(主) / 新浪(备) / 本地缓存兜底
   │      d. eval_candidate()       对每只候选的每一个涨停日做分池评估
   │      e. build_module()         组装 {confirmed, watching} + 近60根K线
   │      f. tag_entries()          与 data.js 要闻/AI预测交叉打标 + 拼 story 底稿
   │
   ├─② 自动化 AI 复核（按 prompt 7.6 步）
   │      · 逐只写 80–180 字 story（板块消息写具体内容；个股财报/业绩/大单优先，可 websearch 核实）
   │      · 补 probability / probNote（基于形态完成度 + 题材热度）
   │      · 禁编造个股公告/财务数据；story 必须非空（自检 e2）
   │
   ├─③ merge_duanban.py draft.json   合并进 data.js 的 duanban 字段
   ├─④ validate_stocks.py           结构校验（8只/N分层/主板/顺序 硬拦；story 软闸门告警）
   └─⑤ deploy.sh                    经 SSH-over-443 推 GitHub Pages 上线
```

---

## 3. 核心形态规则（确认池与观察池共用）

对候选 `code` 的日 K 序列 `kline`（升序），锁定涨停日 `T`（与涨停池口径互验 `pctchg(T) ≥ 涨停阈值`）：

| 步骤 | 条件 | 口径 |
|---|---|---|
| **T 日涨停** | `pctchg(T) ≥ 阈值` | 主板(60/00) 阈值 **9.8%**；创/科(30/68) 阈值 19.8%（但标的范围已排除，见 §6） |
| **T+1 放量断板** | `1.1 ≤ V(T+1)/V(T) ≤ 3.5` **且** `pctchg(T+1) < 阈值` | 温和到强势放量均可，但 **不能续板** |
| **T+2 起缩量** | `0 < V(T+2) < V(T+1)`（必查）；`T+3` 起只要求 `< V(T+1)` | R70 放宽：允许小幅反复，不要求逐日递减 |
| **不破 T 日低点** | `min(close[T+1..最新]) ≥ low(T) × 0.98` | 容差 0.5%；**破位即形态失效，两池均排除** |
| **不破 MA5** | `close(最新) ≥ MA5 × 0.97` | 容差约 3%（确认/待企稳判定用） |

时间窗：`days_ago = 最新索引 − T索引`，要求 `1 ≤ days_ago ≤ 5`；`>5` 视为形态过期；`kline` 不足 8 根直接跳过。

---

## 4. 确认池 vs 观察池（分池判定）

`eval_stage()` 对单只候选的**每个**涨停日评估，取最近一次命中（确认池优先于观察池）：

### ✅ 确认池（`stage = 全流程达标`）
`check_form()` 全流程达标：
- `days_ago ∈ [2, 5]`
- 缩量条件满足（T+2 必缩、T+3 起低于断板日量）
- 不破 T 日低点 **且** 不破 MA5

→ 形态已走完"涨停→放量断板→缩量→不破位"，只待反包。

### 👁 观察池（`pool = watch`，差最后一根确认 K 线）

**观察池·待缩量**（`stage = 待缩量`）：
- `days_ago == 1`
- 放量断板达标、未破 T 日低点，**但还差一根缩量 K 线确认**
- 形态：`9/XX涨停→次2.1倍量断板·待缩量`

**观察池·待企稳**（`stage = 待企稳`）：
- `days_ago ∈ [2, 4]`
- 断板后**已现缩量**、未破 T 日低点，**仅差收复 MA5** 的最后一根确认 K 线
- 形态：`9/XX涨停→次2.1倍量断板→已缩量·待收复MA5`

> 观察池排序：待企稳（更接近确认）排前，同阶段内利空靠后。
> 观察池标的**待最后一根确认 K 线**，确认失败（破位 / 放量不缩 / 跌破 MA5）即移出。

---

## 5. 口径打标（R88 打标制，非硬排除）

`tag_entries()` 把每只入池标的与 `data.js` 的 `bullNews / bearNews / macroNews / intlNews / bankViews / aiPrediction` 交叉核验：

- **利好线索来源**：要闻 `direction=看涨` 的板块/个股、`impacts.stocks` 点名、`aiPrediction` 看多板块及点名个股 → `bull_codes`（个股级）/ `bull_secs`（板块关键词级）
- **利空线索来源**：`bearNews` 或 `direction=看跌` 的板块/个股、`impacts` 点名、`aiPrediction` 看空板块及点名个股 → `bear_codes` / `bear_secs`

**判定（从严）**：
| 结果 | 触发 |
|---|---|
| `bear`（利空） | 个股被利空要闻/AI 看空点名，或所属板块关键词命中看空内容（利好利空同时命中时记 bear） |
| `bull`（利好） | 有利好依据（个股级优先，板块关键词兜底）**且未被看空** |
| `neutral`（中性） | 暂无明确方向依据（AI 可复核板块语义后改判） |

每只附：
- `bullRefs` / `bearRefs`：结构化的命中新闻条目 `{kind:'stock'|'sector', kw, sec, title, summary, source, time, src}`
- `story`（R89 新增）：由命中新闻**自动拼装的小作文底稿**，例如 `板块消息（半导体）：2026-09-22 财联社《xxx》：……` / `个股消息：……`；中性兜底提示"请检索个股公告/财报/大单补充"。**最终个股层面（财报日期、盈利增幅、中标金额等）由自动化 AI 逐只 websearch 改写补全。**

---

## 6. 标的范围与边界

- **仅沪深主板**：代码以 `60` / `00` 开头。
- **排除**：科创板 `688/689`、创业板 `300/301/302`、北交所 `4/8/92` 开头。
- **破 T 日低点** → 形态失效，确认池与观察池**均排除**。
- **days_ago > 5** → 形态过期，排除。
- **涨停池拉取失败（≠空池）** → 该日候选缺失并 `[warn]` 告警，不静默漏候选；限频时退避 3s/6s 重试 2 次，K 线失败冷却 15s 二轮重试。

---

## 7. 数据源（零 MCP，纯 HTTP）

| 数据 | 来源 | 备注 |
|---|---|---|
| 涨停池（含 hybk 行业板块） | 东财 `push2ex.getTopicZTPool` | `date=YYYYMMDD` 无横线，失败重试 |
| 日 K 线（主） | 腾讯 `web.ifzq.gtimg.cn` fqkline | 独立域名、限频少 |
| 日 K 线（备） | 新浪 `CN_MarketData.getKLineData` | 限频窗口兜底 |
| 日 K 线（兜底） | 本地缓存 `kline_cache/` | 两源皆失败启用 |

---

## 8. 前端呈现要点（R89）

- **双池卡片**：确认池 / 观察池，按板块分组、按上涨概率排序；三色标签 利好(红)/利空(绿)/中性(灰)。
- **弹窗**：左右箭头 `←/→` 或 `Esc` 切换/关闭；近 30 根 K 线 + 四均线（MA5/10/20/30，基于全量 60 根计算）。
- **K 线点击看涨幅（R89 新增）**：点击任意红/绿柱 → 浮层显示该根 K 线日期、收盘价、较前收 ±%、成交量（万/亿）；再点同柱或点空白关闭。
- **小作文（R89 新增）**：弹窗"依据"区优先渲染 `story`（按段落拆分 + 来源汇总）；无 story 时按 `bullRefs/bearRefs` 逐条渲染标题与摘要（个股消息 / 板块消息分色）。

---

## 9. 自动化协作口径（R89 prompt）

- **16:00 A股 / 07:30 美股 / 周日23:00 周末** 三份 prompt 的 7.6 / 5.6 / 4.7 步同步要求：逐只写 `story`（80–180 字），板块消息写具体内容，个股财报/业绩/大单优先并 `websearch` 核实，单只超 15 秒放弃，**禁编造**；自检 `e2` 要求 `story` 非空。
- 绝对禁止：第⑪条新增"story 编造个股公告/财务数据"红线。
